using System.Formats.Asn1;
using System.Linq.Expressions;
using SimpleList;

//  Run the tests to visualize program functionality, all implementations work as intended. I won't be doing a console program for it though. 













/*var service = new SimpleListService();

        int option;
        int listSelected;
        bool correctAnswer = false;

        while(correctAnswer != true){
        Console.Clear();
        Console.WriteLine("Welcome to my List implementations. Select 1 for Single link list or Select 2 for Double link list. ");

        if(correctAnswer == true){
            break;
        }
        try{
            int answer = int.Parse(Console.ReadLine());

                if(answer == 1){    
                    listSelected = 1;
                    correctAnswer = true;
                } else if (answer == 2){
                    listSelected = 2;
                    correctAnswer = true;
                }
        } catch {
        }
        }*/

/*
        string input = Console.ReadLine();
        converter.Request(input);

        Console.WriteLine("");
        Console.WriteLine("Press N to exit or any other key to convert another number.");

        string key = Console.ReadLine();
        
            if (key == "n" || key == "N"){

                Console.Clear();
                break;

            }
        }
*/